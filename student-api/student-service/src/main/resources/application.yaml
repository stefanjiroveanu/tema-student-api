spring:
  rabbitmq:
    host: localhost
    port: 5672
    stream:
      username: guest
      password: guest


logging:
  level:
    org: ERROR

server:
  port: 8081

student-api:
  queue: "student_queue"
